# Core
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
pydantic>=2.7.0
pydantic-settings>=2.3.0

# OCR Engine
paddlepaddle-gpu>=2.6.2
paddleocr>=2.9.1,<3.0.0
vietocr>=0.3.5
# VietOCR cần PyTorch. QUAN TRỌNG: dùng bản CPU để tránh xung đột CUDA/pybind
# với paddlepaddle-gpu trong cùng process (Paddle chạy GPU, VietOCR chạy CPU):
#   pip install torch==2.4.1+cpu torchvision==0.19.1+cpu \
#       --index-url https://download.pytorch.org/whl/cpu

# PDF Processing
pdf2image>=1.17.0
Pillow>=10.0.0

# Excel Export
openpyxl>=3.1.0

# Utilities
python-dotenv>=1.0.0
aiofiles>=24.1.0
setuptools>=70.0.0,<81  # <81 để giữ pkg_resources cho gdown/vietocr
numpy>=1.26.0,<2.0.0
opencv-python-headless>=4.9.0
requests>=2.31.0

# Task Queue (optional — uncomment for production)
# celery>=5.4.0
# redis>=5.0.0
